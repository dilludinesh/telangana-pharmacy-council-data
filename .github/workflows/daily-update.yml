name: Data Fetch

on:
  schedule:
    # Run weekdays only during business hours (like a real office worker)
    # Randomized times between 11 AM - 3 PM IST (5:30 AM - 9:30 AM UTC)
    - cron: '45 5 * * 1'   # Monday 11:15 AM IST (5:45 AM UTC)
    - cron: '20 7 * * 2'   # Tuesday 12:50 PM IST (7:20 AM UTC)
    - cron: '55 8 * * 3'   # Wednesday 2:25 PM IST (8:55 AM UTC)
    - cron: '10 6 * * 4'   # Thursday 11:40 AM IST (6:10 AM UTC)
    - cron: '30 9 * * 5'   # Friday 3:00 PM IST (9:30 AM UTC)
  workflow_dispatch:  # Allow manual triggering

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .
    
    - name: Create data directory
      run: |
        mkdir -p data/backups
    
    - name: Check if already updated today
      id: check_update
      run: |
        # Check if we already updated today by looking at recent commits
        TODAY=$(date -u +%Y-%m-%d)
        RECENT_COMMIT=$(git log --oneline --since="$TODAY 00:00:00" --until="$TODAY 23:59:59" --grep="🤖 Daily data update" | head -1)
        
        if [ -n "$RECENT_COMMIT" ]; then
          echo "already_updated=true" >> $GITHUB_OUTPUT
          echo "ℹ️ Already updated today: $RECENT_COMMIT"
        else
          echo "already_updated=false" >> $GITHUB_OUTPUT
          echo "✅ No update today yet, proceeding..."
        fi

    - name: Run daily update
      if: steps.check_update.outputs.already_updated == 'false'
      id: update
      run: |
        echo "🔄 Starting daily TGPC data update during business hours..."
        python -c "
        from tgpc.automation.daily_updater import run_daily_update
        import json
        
        # Run the update
        result = run_daily_update()
        
        # Output results for GitHub Actions
        print(f'SUCCESS={result.success}')
        print(f'TOTAL_RECORDS={result.total_records}')
        print(f'NEW_RECORDS={result.new_records}')
        print(f'REMOVED_RECORDS={result.removed_records}')
        print(f'DUPLICATES_REMOVED={result.duplicates_removed}')
        print(f'INTEGRITY_SCORE={result.data_integrity_score:.3f}')
        
        # Set GitHub Actions outputs
        with open('$GITHUB_OUTPUT', 'a') as f:
            f.write(f'success={result.success}\n')
            f.write(f'total_records={result.total_records}\n')
            f.write(f'new_records={result.new_records}\n')
            f.write(f'removed_records={result.removed_records}\n')
            f.write(f'duplicates_removed={result.duplicates_removed}\n')
            f.write(f'integrity_score={result.data_integrity_score:.3f}\n')
        
        # Create summary
        if result.success:
            print('✅ Daily update completed successfully!')
            print(f'📊 Total records: {result.total_records:,}')
            print(f'🆕 New records: {result.new_records}')
            print(f'🗑️ Removed records: {result.removed_records}')
            print(f'🔍 Duplicates removed: {result.duplicates_removed}')
            print(f'📈 Data integrity: {result.data_integrity_score:.3f}')
        else:
            print('❌ Daily update failed!')
            for error in result.errors:
                print(f'   Error: {error}')
            exit(1)
        "
    
    - name: Check for changes and display new records
      if: steps.check_update.outputs.already_updated == 'false'
      id: changes
      run: |
        if git diff --quiet data/rx.json; then
          echo "changes=false" >> $GITHUB_OUTPUT
          echo "No changes detected in rx.json"
        else
          echo "changes=true" >> $GITHUB_OUTPUT
          echo "Changes detected in rx.json"
          echo ""
          echo "🆕 New Records Added:"
          echo "===================="
          python -c "
        import json
        import subprocess
        
        # Get the diff and extract added lines
        diff_output = subprocess.run(['git', 'diff', 'data/rx.json'], capture_output=True, text=True).stdout
        
        # Parse added records from diff
        new_records = []
        for line in diff_output.split('\n'):
            if line.startswith('+') and not line.startswith('+++') and '{' in line:
                try:
                    # Extract JSON object from the line
                    json_str = line[1:].strip().rstrip(',')
                    record = json.loads(json_str)
                    new_records.append(record)
                except:
                    pass
        
        # Display new records
        if new_records:
            for i, record in enumerate(new_records, 1):
                print(f'{i}. {record.get(\"registration_number\", \"N/A\")} - {record.get(\"name\", \"N/A\")} ({record.get(\"category\", \"N/A\")})')
        else:
            print('No new records could be parsed from diff')
          "
        fi
    
    - name: Update database
      if: steps.check_update.outputs.already_updated == 'false' && steps.changes.outputs.changes == 'true'
      run: |
        echo "📦 Syncing data to SQLite database..."
        python scripts/init_database.py
    
    - name: Commit and push changes
      if: steps.check_update.outputs.already_updated == 'false' && steps.changes.outputs.changes == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Add data files (JSON and database)
        git add data/rx.json data/pharmacists.db
        
        # Create commit message with update details
        git commit -m "🤖 Daily data update - $(date '+%Y-%m-%d')

        📊 Update Summary:
        • Total records: ${{ steps.update.outputs.total_records }}
        • New records: ${{ steps.update.outputs.new_records }}
        • Removed records: ${{ steps.update.outputs.removed_records }}
        • Duplicates removed: ${{ steps.update.outputs.duplicates_removed }}
        • Data integrity: ${{ steps.update.outputs.integrity_score }}
        
        🔄 Automated update using Total Records URL only
        🛡️ Data validated and duplicates removed
        ⏰ Updated at $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
        
        git push
    
    - name: Create update summary
      if: always()
      run: |
        echo "## 📊 Daily TGPC Update Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Date:** $(date '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
        echo "**Status:** ${{ steps.update.outputs.success == 'True' && '✅ Success' || '❌ Failed' }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Results" >> $GITHUB_STEP_SUMMARY
        echo "- **Total Records:** ${{ steps.update.outputs.total_records }}" >> $GITHUB_STEP_SUMMARY
        echo "- **New Records:** ${{ steps.update.outputs.new_records }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Removed Records:** ${{ steps.update.outputs.removed_records }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Duplicates Removed:** ${{ steps.update.outputs.duplicates_removed }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Data Integrity Score:** ${{ steps.update.outputs.integrity_score }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # Display new records if any
        if [ "${{ steps.changes.outputs.changes }}" == "true" ]; then
          echo "### 🆕 New Records" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          python -c "
        import json
        import subprocess
        
        # Get the diff and extract added lines
        diff_output = subprocess.run(['git', 'diff', 'data/rx.json'], capture_output=True, text=True).stdout
        
        # Parse added records from diff
        new_records = []
        for line in diff_output.split('\n'):
            if line.startswith('+') and not line.startswith('+++') and '{' in line:
                try:
                    json_str = line[1:].strip().rstrip(',')
                    record = json.loads(json_str)
                    new_records.append(record)
                except:
                    pass
        
        # Display new records in markdown table
        if new_records:
            print('| Registration | Name | Father Name | Category |')
            print('|--------------|------|-------------|----------|')
            for record in new_records:
                reg = record.get('registration_number', 'N/A')
                name = record.get('name', 'N/A')
                father = record.get('father_name', 'N/A')
                category = record.get('category', 'N/A')
                print(f'| {reg} | {name} | {father} | {category} |')
        else:
            print('_No new records could be parsed_')
          " >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "### Data Source" >> $GITHUB_STEP_SUMMARY
        echo "- **URL:** https://www.pharmacycouncil.telangana.gov.in/pharmacy/srchpharmacisttotal" >> $GITHUB_STEP_SUMMARY
        echo "- **Method:** Single request to Total Records (server-friendly)" >> $GITHUB_STEP_SUMMARY
        echo "- **Fields:** serial_number, registration_number, name, father_name, category" >> $GITHUB_STEP_SUMMARY
    
    - name: Notify on failure
      if: failure()
      run: |
        echo "❌ Daily update failed! Check the logs above for details."
        echo "The rx.json file was not updated due to validation failures or errors."